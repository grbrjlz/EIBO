package structure;

public enum Commands {

    //Befehle an Backend.Mp3Player:
    play, pause, stop, quit, volume, info,

    //Befehle an Backend.KeyboardController:
    settings,

    //Befehle an Backend.PlaylistManager:
    skip, back, shuffle, repeat, set, commands


}
